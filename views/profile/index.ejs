<%- include('../partials/header.ejs')%>
<div class="proff">
<div class="profileHeader">
        <img class="profileimg" src="<%= Users.find(elem=>elem.id==user.id).avatar%>">
        <h1><%= Users.find(elem=>elem.id==user.id).name%></h1>
    </div>
    <hr class="ln"/>
    <div class="buttonDiv"><button class="profilBtn b1">All POSTS</button> <a href="/posts/new"><button class="profilBtn">NEW POST</button></a></div>
<div class="profileP">
    <%if(Posts.length==0){%>
        <p class="profileHeader">No post yet</p>
        <%}else{%>
        <%Posts.forEach(post=>{%>
            <div class="profilePost"><a href="/posts/<%= post.id%>"><img class="imgProfile" src=<%=post.imageUrl%> alt=''/></a></div>
    <%})}%> 
        
        
        
</div>

</div>

<%- include('../partials/footer.ejs')%>